import { json } from "../utils";

export async function handleRelated(_spDc: string, _browseId: string): Promise<Response> {
  return json([]);
}

export async function handleRelatedForTrack(_spDc: string, _trackId: string): Promise<Response> {
  return json([]);
}
